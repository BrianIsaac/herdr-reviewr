# Cockpit fork surfaces: Codex handover

9 September 2026. Research artefact: [implementation map](../../research/cockpit-fork-surfaces-codex-2026-09-09.md). Source baseline: `4c090225af706bf3aaa24b39fea890a72994f40f`, version `0.36.2` (`Cargo.toml:3`, `herdr-plugin.toml:3`). No product behaviour was implemented.

Surveyed the required repository guidance, README, recipes, lint/dependency policy, all existing `docs/` guides/specs, manifest and shell action; verified both earlier briain maps against this checkout. Traced the CLI/config/startup/recovery, App/UI picker, git/world base, herdr agent resolution, export/comment model and their adjacent tests. Read the registered project schema, this project's frontmatter and this job's status/job association; no other active agent's work was inspected or coordinated. Source roots and corrected prior claims are indexed in `docs/research/cockpit-fork-surfaces-codex-2026-09-09.md:5–18`.

The three riskiest points:

1. **Selecting after repository ownership is established.** The shell rejects non-git cwd; Rust constructs App before terminal setup, starts the world worker on one root, and recovery rebuilds from `cfg`. A picker added only to `Mode` leaves some of those paths attached to the cockpit. Finalise the selected root in configuration before review startup and worker creation (`herdr/pane.sh:236–249`, `src/lib.rs:72–83`, `src/lib.rs:893–907`, `src/lib.rs:1577–1583`; map:22–42).
2. **Routing or consuming the wrong review.** Resolve an explicit cockpit/name/pane against the full agent list, reject absent/ambiguous targets, and preserve the legacy unset path. Format one shared header before bracketed paste. A failed send retains comments; successful injection followed by failed focus still consumes them exactly once (`src/herdr.rs:234–266`, `src/herdr.rs:391–432`, `src/export.rs:141–147`, `src/app.rs:4724–4744`; map:72–116).
3. **Reporting a base or run identity the diff never used.** Local main differs semantically from the origin-first base picker; `status.json` does not carry this job's project id. Join job YAML and use git identity, define upstream explicitly, and carry truthful header context across scopes/recovery. Do not replace the generic upstream-tested base chain with briain policy (`src/git.rs:592–636`, `src/git.rs:903–932`, `src/world.rs:121–145`; map:44–62, map:108–116).

Next implementer: write `docs/specs/2026-09-09-briain-cockpit-flow/spec.md`, plan and five tickets, following `docs/specs/README.md:3–6`. Ratify the map's proposed bare-selector bootstrap, tracking-ref interpretation of upstream, Branch startup, pinned base, ambiguity and existing-pane policies before coding. The five suggested feature commits and six-to-nine-sitting estimate are at `docs/research/cockpit-fork-surfaces-codex-2026-09-09.md:225–233`; setup/gate/link/rebase instructions are at that file's lines 146–206.

Validation is static source/reference checking and `git diff --check`. Gate times are explicitly unmeasured estimates. No Rust installation, build/test execution, linking, plugin/config/keybinding edits, pane opens or subagent calls were performed. The initial checkout already had only the generated `AGENTS.md` brief as a tracked modification; it is preserved and excluded from the research commits because the brief says it is never merged (`AGENTS.md:62–65`). Thus the requested clean-status condition conflicts with preserving that pre-existing file; do not conceal it using index flags or commit the brief as research output.
